<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardBackgroundColor="@color/toolbar_background_color"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="12dp">

        <ImageView
            android:id="@+id/device_image"
            android:layout_width="0dp"
            android:layout_height="120dp"
            android:background="@android:color/white"
            android:scaleType="fitCenter"
            android:padding="4dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <ProgressBar
            android:id="@+id/image_progress_bar"
            style="?android:attr/progressBarStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="@+id/device_image"
            app:layout_constraintBottom_toBottomOf="@+id/device_image"
            app:layout_constraintStart_toStartOf="@+id/device_image"
            app:layout_constraintEnd_toEndOf="@+id/device_image" />

        <TextView
            android:id="@+id/device_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:textAppearance="?attr/textAppearanceSubtitle1"
            android:textStyle="bold"
            app:layout_constraintTop_toBottomOf="@+id/device_image"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="Device Name" />

        <!-- 1. Specific UI Container below Name -->
        <FrameLayout
            android:id="@+id/device_specific_ui_container"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="0dp"
            android:background="@color/white"
            app:layout_constraintTop_toBottomOf="@+id/device_name"
            app:layout_constraintStart_toStartOf="@+id/device_name"
            app:layout_constraintEnd_toEndOf="@+id/device_name" />

        <!-- 2. Status and Location grouped in a LinearLayout below the Container -->
        <LinearLayout
            android:id="@+id/status_location_layout"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            app:layout_constraintTop_toBottomOf="@+id/device_specific_ui_container"
            app:layout_constraintStart_toStartOf="@+id/device_name"
            app:layout_constraintEnd_toEndOf="@+id/device_name">

            <ImageView
                android:id="@+id/device_status_icon"
                android:layout_width="18dp"
                android:layout_height="18dp"
                tools:src="@drawable/ic_baseline_wifi_24"
                app:tint="@color/black" />

            <TextView
                android:id="@+id/device_location"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:textAppearance="?attr/textAppearanceBody2"
                android:textColor="?android:attr/textColorSecondary"
                tools:text="Living Room" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>